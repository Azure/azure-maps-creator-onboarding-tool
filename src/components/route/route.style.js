import { css } from '@emotion/css';

export const routeStyle = css`
  margin: 0.5rem 1.25rem;
  padding-bottom: 3.5rem; // this was added to make sure footer never overlaps content, especially on small mobile screens
`;