import { fontSize } from 'common/styles';

export const inputStyles = {
  fieldGroup: [{
    height: '1.5rem',
  }],
  field: [{
    fontSize: fontSize.sm,
  }]
};

export const inputStylesCentered = {
  fieldGroup: [{
    height: '1.5rem',
  }],
  field: [{
    padding: 0,
    textAlign: 'center',
    fontSize: fontSize.sm,
  }]
};